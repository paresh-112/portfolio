version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
